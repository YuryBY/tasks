
hibernate.factory.close = Hibernate SessionFactory is closed

jdbc.connection.close = Close JDBC connections

jpa.entitymanager.null = EntityManager is null
jpa.factory.close      = JPA EntityManagerFactory is closed

pool.empty = Connection pool is empty

employee.lost = Employee with given id not founded in given list

jdbc.connection.null = Connection is null

tag.value.countOnPage = Not integer number where gotten